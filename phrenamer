#!/bin/sh

[ "$1" ] && dir=$1 || dir="."
files=$(ls -1 "$dir" | grep -E '\.(jpg|JPG|jpeg)$')

IFS="
"

count=0
for fname in $files; do
	count=$((count+1))
	newname="$(printf %02d $count).jpg"
	! [ -f "$dir/$newname" ] && mv "$dir/$fname" "$dir/$newname"
done

